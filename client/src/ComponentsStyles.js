const styles = {
    chartContainer : {
        width: "99%",
        display : "flex",
        justifyContent: "center",
        alignItems: "center",
        flexDirection: "column",
    }
}

export default styles;